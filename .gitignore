# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environment (never commit installed packages)
venv/
.venv/

# Local secrets / config (commit .env.example, not .env)
.env

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Personal interview-prep notes — must NOT be pushed to the public submission repo
INTERVIEW_PREP.*
scripts/md_to_pdf.py
